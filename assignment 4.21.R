# write a function to merge two list into one nested list
#without loosing the original list structures

list_a<-list(name="Shivani",age=20)
list_a
list_b<-list(city="Patna",country="India")
list_b
merged<-function(list_a,list_b)
merged
